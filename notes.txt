Box-sizing example:

If an element has:
- width: 200px
- padding: 20px
- border: 10px

Without box-sizing: border-box
- total rendered width = 200 + 20 + 20 + 10 + 10 = 260px

With box-sizing: border-box
- total rendered width stays 200px
- content area becomes 140px

This makes element sizing more predictable.

Grid note: auto-fit + minmax(15rem, 1fr) creates as many columns as fit while keeping each card at least 15rem wide and sharing the remaining space.

Example box sizing comparison:

If an element is styled as:

- width: 200px
- padding: 20px
- border: 10px

Then the rendered box width is:

- Without box-sizing: border-box
  - total width = 200px + 20px + 20px + 10px + 10px = 260px
- With box-sizing: border-box
  - total width stays 200px
  - content box becomes 200px - 20px - 20px - 10px - 10px = 140px

This is why border-box makes layout sizing more predictable.

Responsive checks and screenshots

- Mobile screenshot: screenshot-mobile.png (390 x 844, full page)
- Desktop screenshot: screenshot-desktop.png (1280 x 900, full page)
- Mobile navigation starts closed, opens from the menu button, and closes with Escape or a navigation link.
- Desktop navigation is visible and the menu button is hidden at 1280px.
- Desktop uses a single body grid column, keeping the main content and hero centered instead of reserving an empty sidebar.
- Archive photo loads from favicon.jpg; its alt text and caption describe the kitten shown.

Latest viewport test (2026-09-26)

- Phone capture: responsive-phone.png (390 x 900)
- Desktop capture: responsive-desktop.png (1440 x 900)
- At 390px, the 40rem and 64rem media queries are off; hamburger nav is shown and events use one column.
- At 768px, the 40rem query is on; full nav is shown and events use two columns.
- At 1440px, both queries are on; full nav is shown and events use three columns.

Event-board assignment answers

1. The :root palette and design tokens are:

  --brand: #2c3e50;
  --brand-deep: #1a252f;
  --brand-soft: #546e7a;
  --accent: #ffe08a;
  --accent-strong: #f39c12;
  --accent-soft: rgba(243, 156, 18, 0.2);
  --ink: rgb(34 34 34);
  --ink-soft: hsl(0 0% 33%);
  --paper: rgb(255 255 255);
  --paper-alt: #f4f4f4;
  --paper-muted: #f9fafb;
  --line: #eeeeee;
  --line-strong: hsl(210 27% 81%);
  --field-bg: #f9fbff;
  --link-visited: rgb(84 110 122);
  --link-active: #b94c00;
  --focus: hsl(38 90% 50%);
  --banner-overlay: rgb(0 0 0 / 40%);

  Named variables make colors consistent, communicate intent (for example, --paper versus --ink), and allow a palette change in one place instead of searching for repeated hex codes.

2. Hex example: #2c3e50 is the brand/nav color. RGB example: rgb(34 34 34) is the main ink color. HSL example: hsl(0 0% 33%) is muted text. Semi-transparent colors include --accent-soft at 20% opacity for focus rings, and --banner-overlay at 40% opacity for the dark hero image overlay; transparency lets the underlying color/image show through.

3. rem units scale from the root font size, preserving proportional spacing and type when a user changes their default text size. The main-width calculation is width: min(calc(100% - 2rem), 60rem); it leaves 1rem gutters on both sides until the content reaches its 60rem cap. The heading uses font-size: clamp(1.5rem, 4vw, 3rem); it grows with viewport width but stays between readable minimum and maximum sizes.

4. With content-box, rendered width is 200 + 20 + 20 + 5 + 5 = 250px. With border-box, rendered width remains 200px and the content area is 150px. The stylesheet applies box-sizing: border-box globally so declared dimensions include padding and borders and are easier to reason about.

5. From inside out: content, padding, border, margin.

6. repeat(auto-fit, minmax(15rem, 1fr)) creates as many event-card columns as fit, with each track at least 15rem and available extra space shared evenly. Auto-fit and minmax calculate the track count from available space, so this grid adapts without a media query.

7. Mobile-first CSS sets the simplest phone layout as the default, then min-width queries progressively add columns and desktop navigation as space becomes available. It avoids overriding a desktop-first layout on small screens. The viewport meta tag, <meta name="viewport" content="width=device-width, initial-scale=1" />, makes the CSS viewport match the device width and prevents mobile browsers from rendering a wide virtual page and shrinking it.

8. The page grid-template-areas map is:

  "header"
  "nav"
  "main"
  "footer"

  Each row has one column, so no region spans multiple columns in the final centered layout. In a multi-column map, a region spans columns by repeating its area name in adjacent tracks (for example, "header header"); this page has no sidebar requiring that arrangement.

9. Images use max-width: 100% and height: auto, so they shrink within their container without distortion and need no media query. Type uses clamp(), which fluidly scales headings between fixed minimum and maximum sizes without a media query.

10. Repository: https://github.com/somil9998/event-board. GitHub Pages: https://somil9998.github.io/event-board/. The public repository already had README.md and a readable two-commit history when checked; this content update adds a separate descriptive commit. The final index.html and style.css are in the repository. README.md documents the static site, files, responsive behavior, and Pages deployment.

Appendix A: Final index.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Envento — Event Management Club, IIIT Vadodara Gandhinagar</title>
    <meta
      name="description"
      content="Envento is the event management club of IIIT Vadodara, Gandhinagar Campus. See upcoming events, past events, and how to join."
    />
    <link rel="icon" href="favicon.jpg" />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <meta property="og:title" content="Envento — Event Management Club" />
    <meta
      property="og:description"
      content="Upcoming campus events, fests, and how to join Envento."
    />
    <meta property="og:image" content="favicon.jpg" />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <a href="#main" class="skip">Skip to content</a>

    <header class="site-header">
      <a href="#hero" class="logo">Envento</a>
      <button class="nav-toggle" type="button" aria-label="Toggle navigation" aria-expanded="false" aria-controls="primary-navigation">
        <span></span>
        <span></span>
        <span></span>
      </button>
      <nav id="primary-navigation" aria-label="Primary" class="main-nav nav-links">
        <a href="#events">Events</a>
        <a href="#about">About</a>
        <a href="#team">Team</a>
        <a href="#join">Join</a>
      </nav>
      <span class="hidden-on-load">This is hidden with display: none.</span>
    </header>

    <main id="main" class="page-content">
      <section id="hero" class="hero">
        <h1>Envento</h1>
        <p>
          Event Management Club, <span lang="hi">IIIT Vadodara</span> —
          Gandhinagar Campus.
        </p>
        <p>We plan and run events across campus, from technical fests to cultural nights.</p>
        <a href="#events" class="button">See upcoming events</a>


          <p>48 events run since 2022.</p>
          <p>200+ active volunteers.</p>
      </section>

      <section id="events" class="events-section">
        <h2>Upcoming Events</h2>
        <ul>
          <li>
            <article>
              <h3>Technova</h3>
              <p>A technical event for coders, builders and makers.</p>
              <p>
                <time datetime="2026-09-12">12 September 2026</time> ·
                Main Auditorium
              </p>
              <p><mark>Registration open</mark></p>
              <a href="event-technova.html">View event</a>
            </article>
          </li>
          <li>
            <article>
              <h3>Echoes</h3>
              <p>A cultural night of music and performance.</p>
              <p>
                <time datetime="2026-09-20">20 September 2026</time> ·
                Open Air Stage
              </p>
              <p>Registration opens soon.</p>
              <a href="event-echoes.html">View event</a>
            </article>
          </li>
          <li>
            <article>
              <h3>Athena</h3>
              <p>An inter-college competition testing strategy and speed.</p>
              <p>
                <time datetime="2026-10-04">4 October 2026</time> ·
                Seminar Hall
              </p>
              <p><mark>Registration open</mark></p>
              <a href="event-athena.html">View event</a>
            </article>
          </li>
        </ul>
      </section>

      <section id="gallery" class="gallery">
        <h2>From the Archive</h2>
        <figure>
          <img
            src="favicon.jpg"
            alt="A white kitten standing in a grassy field"
          />
          <figcaption>A white kitten in the grass</figcaption>
        </figure>
      </section>

      <section id="about" class="about">
        <h2>About Envento</h2>
        <p>
          Envento is the event management club of
          <abbr title="Indian Institute of Information Technology Vadodara">IIIT Vadodara</abbr>,
          Gandhinagar Campus. We plan, coordinate and run events for the
          student community.
        </p>
        <p>
          The club's work is summarised each year in the
          <cite>Envento Annual Report</cite>.
        </p>

        <h3>Frequently Asked Questions</h3>
        <details>
          <summary>Who can join Envento?</summary>
          <p>Any student at IIIT Vadodara, Gandhinagar Campus, can join. No prior experience is required.</p>
        </details>
        <details>
          <summary>How do I register for an event?</summary>
          <p>
            Use the <kbd>Register</kbd> button on each event page, or fill
            out the join form below.
          </p>
        </details>
      </section>

      <section id="team" class="team">
        <h2>Team</h2>
        <ul>
          <li>Aarohi Shah — President</li>
          <li>Ritvik Mehta — Vice President</li>
          <li>Devika Nair — Event Head</li>
        </ul>
      </section>

      <section id="join" class="join-section">
        <h2>Join Envento</h2>
        <p>Fill in your details and we will get in touch.</p>
        <form>
          <label for="name">Full name</label>
          <input type="text" id="name" name="name" required />

          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />

          <label for="date">Preferred date</label>
          <input type="date" id="date" name="date" />

          <label for="interest">Which team interests you?</label>
          <select id="interest" name="interest">
            <option value="technical">Technical</option>
            <option value="cultural">Cultural</option>
            <option value="operations">Operations</option>
          </select>

          <label for="message">Message (optional)</label>
          <textarea id="message" name="message" placeholder="Tell us what you are interested in..."></textarea>

          <button type="submit">Send</button>
        </form>
      </section>
    </main>

    <footer>
      <p>© 2026 Envento — IIIT Vadodara</p>
      <nav aria-label="Footer" class="footer-links">
        <a href="#events" class="active">Events</a>
        <a href="#about">About</a>
        <a href="#team">Team</a>
      </nav>

      <table class="event-table">
        <thead>
          <tr>
            <th>Event</th>
            <th>Month</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Technova</td>
            <td>September</td>
          </tr>
          <tr>
            <td>Echoes</td>
            <td>September</td>
          </tr>
          <tr>
            <td>Athena</td>
            <td>October</td>
          </tr>
        </tbody>
      </table>
      <address>
        Contact us at
        <a href="mailto:envento@iiitvadodara.ac.in"><code>envento@iiitvadodara.ac.in</code></a>
        <br />
        <a href="https://iiitvadodara.ac.in" target="_blank" rel="noreferrer">Visit IIIT Vadodara</a>
      </address>
    </footer>
    <script>
      const navToggle = document.querySelector(".nav-toggle");
      const primaryNavigation = document.querySelector("#primary-navigation");

      function closeNavigation() {
        navToggle.setAttribute("aria-expanded", "false");
        primaryNavigation.classList.remove("is-open");
      }

      navToggle.addEventListener("click", () => {
        const isExpanded = navToggle.getAttribute("aria-expanded") === "true";
        navToggle.setAttribute("aria-expanded", String(!isExpanded));
        primaryNavigation.classList.toggle("is-open", !isExpanded);
      });

      primaryNavigation.addEventListener("click", (event) => {
        if (event.target.closest("a")) closeNavigation();
      });

      document.addEventListener("keydown", (event) => {
        if (event.key === "Escape") closeNavigation();
      });
    </script>
  </body>
</html>
```

Appendix B: Final style.css

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --brand: #2c3e50;
  --brand-deep: #1a252f;
  --brand-soft: #546e7a;
  --accent: #ffe08a;
  --accent-strong: #f39c12;
  --accent-soft: rgba(243, 156, 18, 0.2);
  --ink: rgb(34 34 34);
  --ink-soft: hsl(0 0% 33%);
  --paper: rgb(255 255 255);
  --paper-alt: #f4f4f4;
  --paper-muted: #f9fafb;
  --line: #eeeeee;
  --line-strong: hsl(210 27% 81%);
  --field-bg: #f9fbff;
  --link-visited: rgb(84 110 122);
  --link-active: #b94c00;
  --focus: hsl(38 90% 50%);
  --banner-overlay: rgb(0 0 0 / 40%);

  --font-body: "Inter", "Segoe UI", sans-serif;
  --font-display: "Playfair Display", Georgia, serif;
  --type-body: 1rem;
  --type-small: 0.875rem;
  --type-h3: 1.25rem;
  --type-h2: 1.75rem;
  --type-h1: clamp(1.5rem, 4vw, 3rem);

  --text: var(--ink);
  --text-muted: var(--ink-soft);
  --bg: var(--paper);
  --bg-alt: var(--paper-alt);
  --border: var(--line);
  --brand-dark: var(--brand-deep);
}

/* Base page styling */
body {
  font-family: var(--font-body);
  color: var(--ink);
  background-color: var(--paper);
  line-height: 1.5;
  display: grid;
  grid-template-areas:
    "header"
    "nav"
    "main"
    "footer";
  grid-template-columns: 1fr;
  grid-template-rows: auto auto 1fr auto;
}

img {
  display: block;
  max-width: 100%;
  height: auto;
}

figure img,
.hero {
  object-fit: cover;
}

h1,
h2,
h3 {
  font-family: var(--font-display);
  color: var(--brand);
}

h1 {
  font-size: var(--type-h1);
  margin: 0 0 0.5rem;
  letter-spacing: -0.02em;
}

h2 {
  font-size: clamp(1.5rem, 3vw, 2.2rem);
  margin-top: 2rem;
  border: none;
  border-bottom: 2px solid var(--line);
  padding-bottom: 0.25rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

h3 {
  font-size: var(--type-h3);
  margin-bottom: 0.25rem;
}

p {
  margin: 0.5rem 0;
  max-width: 65ch;
}

.site-header .logo,
.main-nav a,
.hero .button,
form button,
footer a,
.events-section li article p,
.about p,
.join-section p {
  font-size: clamp(0.95rem, 1.5vw, 1.125rem);
}

mark {
  background: var(--accent);
  padding: 0 0.25rem;
}

abbr {
  cursor: help;
}

/* Link state styling: link, visited, hover, active, focus */
a {
  color: var(--brand);
  text-decoration: none;
  transition: color 0.2s ease, text-decoration-color 0.2s ease;
}

a:link {
  color: var(--brand);
}

a:visited {
  color: var(--brand-soft);
}

a:hover {
  color: var(--brand-deep);
  text-decoration: underline;
  text-decoration-thickness: 0.125rem;
}

a:active {
  color: var(--link-active);
  text-decoration: underline;
}

a:focus-visible {
  outline: 0.1875rem solid var(--focus);
  outline-offset: 0.1875rem;
  border-radius: 0.25rem;
  text-decoration: underline;
}

button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 0.1875rem solid var(--focus);
  outline-offset: 0.125rem;
  box-shadow: 0 0 0 0.25rem var(--accent-soft);
}

.skip {
  position: absolute;
  left: -62.5rem;
  top: 0;
  background: var(--brand);
  color: var(--paper);
  padding: 0.5rem 1rem;
  z-index: 10;
}

.skip:focus {
  left: 0;
}

/* Header layout */
.site-header {
  grid-area: header;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 2rem;
  background-color: var(--brand);
}

.main-nav {
  grid-area: nav;
  display: none;
  gap: 1rem;
  background-color: var(--brand-deep);
  padding: 0.75rem 2rem;
}

.nav-links {
  display: none;
}

.main-nav.is-open {
  display: flex;
  flex-direction: column;
}

.nav-toggle {
  display: inline-flex;
  flex-direction: column;
  justify-content: center;
  gap: 0.25rem;
  width: 2.5rem;
  height: 2.5rem;
  border: 1px solid rgba(255, 255, 255, 0.5);
  border-radius: 0.25rem;
  background: transparent;
  cursor: pointer;
}

.nav-toggle span {
  display: block;
  width: 1.25rem;
  height: 2px;
  background-color: var(--paper);
  margin: 0 auto;
}

@media (min-width: 40rem) {
  .events-section ul {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .main-nav {
    display: flex;
    flex-direction: row;
  }

  .nav-toggle {
    display: none;
  }

  .nav-links {
    display: flex;
  }
}

@media (min-width: 64rem) {
  body {
    grid-template-areas:
      "header"
      "nav"
      "main"
      "footer";
    grid-template-columns: minmax(0, 1fr);
  }

  .main-nav {
    justify-content: center;
  }
}

.logo,
.main-nav a,
.hero .button,
form button {
  transition: opacity 0.2s ease, transform 0.2s ease;
}

.logo {
  color: var(--paper);
  font-weight: bold;
  font-size: 1.25rem;
  text-decoration: none;
}

/* Descendant selector: any link inside the nav */
.main-nav a {
  color: var(--paper);
  text-decoration: none;
  margin-left: 1rem;
}

.main-nav a:hover {
  text-decoration: underline;
}

/* Child combinator: only direct section elements inside main are targeted */
.page-content > section {
  margin: 2rem 0 0 0;
}

/* Adjacent sibling selector: styles only the first paragraph immediately after an h2 */
h2 + p {
  margin-top: 0.5rem;
  color: var(--ink-soft);
}

/* General sibling selector: styles all paragraphs that follow an h2, not just the next one */
h2 ~ p {
  line-height: 1.7;
}

main {
  grid-area: main;
  width: min(calc(100% - 2rem), 60rem);
  margin: 0 auto;
  padding: 1rem 2rem 3rem 2rem;
}

.hidden-on-load {
  display: none;
}

/* Footer layout */
footer {
  grid-area: footer;
  background-color: var(--paper-alt);
  text-align: center;
  padding: 2rem;
}

footer nav a {
  margin: 0 0.5rem;
  color: var(--brand);
  text-decoration: underline;
}

.footer-links a:not(.active) {
  opacity: 0.85;
}

.footer-links a:not(.active)::after {
  content: " •";
  margin-left: 0.5rem;
}

.footer-links a:last-child::after {
  content: "";
}

/* Position selectors: nth-child, first-child, last-child */
.event-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 1.5rem;
}

.event-table th,
.event-table td {
  border: 1px solid var(--border);
  padding: 0.75rem;
  text-align: left;
}

.event-table tbody tr:nth-child(even) {
  background-color: var(--paper-muted);
}

.event-table tbody tr:first-child td {
  border-top: 2px solid var(--brand);
}

footer address {
  font-style: normal;
  margin-top: 0.5rem;
}

/* Hero section styling */
.hero {
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  min-height: 28rem;
  padding: 4rem 2rem 3rem;
  background-image: linear-gradient(var(--banner-overlay), var(--banner-overlay)), url("favicon.jpg");
  background-repeat: no-repeat;
  background-size: 6rem, cover;
  background-position: top right, center;
}

.hero .button {
  display: inline-block;
  background-color: var(--brand);
  color: var(--paper);
  text-decoration: none;
  padding: 0.6rem 1.2rem;
  border-radius: 0.25rem;
  margin-top: 1rem;
}

.hero .button:hover {
  background-color: var(--brand-deep);
}

.hero aside {
  margin-top: 2rem;
  font-size: 0.9rem;
  color: var(--ink-soft);
}

/* Event section card styling */
.events-section {
  background-color: var(--paper-alt);
  border: 2px solid var(--line);
  border-radius: 0.5rem;
  padding: 1.5rem 2rem;
  margin: 2rem 0 0 0;
}

.events-section ul,
.team ul {
  list-style: none;
}

.events-section ul {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  gap: 1rem;
}

.events-section li {
  background-color: var(--paper);
  border: 1px solid var(--line);
  border-radius: 0.5rem;
  padding: 1rem 1.25rem;
  margin: 0;
  box-shadow: 0 2px 8px rgb(0 0 0 / 8%);
}

.team li {
  padding: 0.25rem 0;
}

figure {
  margin: 0;
}

figure img {
  max-width: 100%;
  border-radius: 0.25rem;
}

figcaption {
  font-size: 0.9rem;
  color: var(--ink-soft);
  margin-top: 0.25rem;
}

form {
  display: flex;
  flex-direction: column;
  max-width: 25rem;
}

form label {
  margin-top: 0.75rem;
  font-weight: bold;
}

/* External links open to a different website get a visible indicator */
a[href^="https"]::after {
  content: " ↗";
  font-size: 0.8em;
  display: inline-block;
  margin-left: 0.15rem;
}

/* Typed form fields can be styled separately by their type */
[type="email"],
[type="date"] {
  border: 2px solid var(--line-strong);
  background-color: var(--field-bg);
}

form input,
form select,
form textarea {
  padding: 0.5rem;
  margin-top: 0.25rem;
  border: 1px solid var(--line-strong);
  border-radius: 0.25rem;
  font: inherit;
}

form textarea::placeholder {
  color: var(--ink-soft);
  font-style: italic;
}

form button {
  margin-top: 1.25rem;
  align-self: flex-start;
  background-color: var(--brand);
  color: var(--paper);
  border: none;
  padding: 0.6rem 1.2rem;
  border-radius: 0.25rem;
  cursor: pointer;
}

form button:hover {
  background-color: var(--brand-deep);
}

/* Generated content with ::before and ::after */
blockquote {
  position: relative;
  margin: 1.5rem 0;
  padding: 1rem 1rem 1rem 2.5rem;
  background: var(--bg-alt);
  border-left: 4px solid var(--brand);
  border-radius: 0.375rem;
}

blockquote::before {
  content: "“";
  position: absolute;
  left: 0.8rem;
  top: 0.2rem;
  font-size: 2rem;
  color: var(--brand);
}

/* Inherit: keep the value from the parent element */
.hero .button {
  color: inherit;
}

/* Initial: reset to the browser default for that property */
.footer-links a {
  color: initial;
}

/* Unset: remove the value so inheritance or browser default can take over */
.event-table th {
  border: unset;
}

blockquote::after {
  content: "”";
  margin-left: 0.2rem;
  color: var(--brand);
}
```
